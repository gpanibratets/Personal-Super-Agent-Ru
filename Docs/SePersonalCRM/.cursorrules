# SePersonalCRM - Personal CRM для команды PropTech

**Назначение**: Персональный CRM для управления взаимоотношениями с партнерами и клиентами команды PropTech в Alatau City Bank.

**Владельцы**: PropTech (Alatau City Bank)

---

## 📚 Быстрая справка для команды

**Для ВСЕХ членов команды, использующих Cursor AI**: См. **[TEAM_QUICK_REFERENCE.md](./TEAM_QUICK_REFERENCE.md)** для:
- Как запросить исследование контакта (веб/LinkedIn)
- Общие команды простым языком
- Функции автоматического обновления
- Git операции
- Структура файлов и рабочие процессы

**Почему существует это руководство**: Память Cursor привязана к аккаунту (не делится между участниками команды). Это руководство гарантирует, что все знают, как использовать систему CRM независимо от их индивидуальной конфигурации Cursor.

**Быстрые команды**:
- "do web research for [Person Name]" → Исследование людей через Perplexity API
- "do company research for [Company Name]" → Исследование компаний через Perplexity API
- "research: [any topic]" → Общее исследование через Perplexity API
- "do linkedin research for [Person Name]" → Сканирование профиля LinkedIn
- "update all" → Проверка статуса Git
- "commit" → Git add + commit + push

---

## 🎯 КРИТИЧЕСКИ ВАЖНО: Контекст банка и продуктов

**Работа с партнерами и клиентами требует знаний о банке, продуктах и услугах.**

Если нужен контекст о банке или продуктах, загрузите соответствующие документы:

```
Read: ../Strategy/.cursorrules (если есть)
Или используйте информацию из профилей компаний и контактов
```

**Почему**: Переговоры с партнерами, презентации продуктов, обсуждение интеграций, работа с клиентами - все требует актуального контекста о банке, продуктах PropTech и возможностях интеграции.

---

## Активная рабочая папка

**Основная папка**: `MainCRM/` - Вся активная работа с партнерами и клиентами происходит здесь

**НЕ ИСПОЛЬЗУЙТЕ**: `[Legacy_Folder_A]/` или `[Legacy_Folder_B]/` - только данные разработки/архива

---

## Обновления репозитория

**"Update CRM"** = `git pull` этого репозитория (не синхронизация email/Telegram).

---

## Обработка входящих и управление задачами

**Для ВСЕЙ работы по обработке входящих и задач**, прочитайте полное руководство:

```
MainCRM/Inbox/inbox_processing_rules.md
```

**Когда читать:**
- Обработка AI inbox, email, Telegram, LinkedIn
- Работа с processing_tasks.md
- Создание рабочих задач
- Понимание двухэтапного рабочего процесса
- Любые вопросы, связанные с входящими

**Ключевая концепция:** Двухэтапная система (сначала собрать ВСЕ входящие, затем обработать с полным контекстом)

---

## Руководящие принципы планирования и анализа AI-ассистента

**Оценка времени:**

- **ВСЕГДА указывайте**, для кого предназначены оценки времени:
  - **Выполнение человеком**: Время, необходимое человеку для выполнения задачи вручную
  - **Выполнение AI-агентом**: Время, необходимое AI для выполнения задачи (обычно намного быстрее)
- Формат: "X часов (человек)" или "X минут (AI-агент)" или "X часов человек / Y минут AI"
- Пример: "Фаза 1: Анализ (30 мин AI / 2 часа человек)"

**Проверка содержимого папок и файлов:**

- **ВСЕГДА проверяйте содержимое папок** перед тем, как делать предположения на основе названий папок
- **Проверяйте тип сущности**: Даже если папка названа в честь компании, проверьте, содержит ли она информацию о компании или личную информацию о контакте
- **Проверяйте папки Person+Company**: Для папок с именами и человека, и компании проверьте содержимое, чтобы определить основную сущность (профиль человека vs. профиль компании)
- **Не полагайтесь только на названия**: Названия папок могут не соответствовать фактическому содержимому

**Классификация компаний (SePersonalCRM):**

- При документировании принадлежности к компаниям в профилях людей используйте префиксы для указания типа компании:
  - **"Partner:"** - Для партнерских компаний (технологические партнеры, интеграторы, поставщики услуг, платформы)
  - **"Client:"** - Для клиентских компаний (компании, использующие продукты/услуги банка)
  - **"Firm:"** - Для коммерческих операционных компаний (стартапы, бизнесы, где люди являются основателями, CEO или сотрудниками)
- Примеры:
  - "Partner: [TECH_PARTNER_A]" (технологический партнер)
  - "Partner: [INTEGRATION_PLATFORM_B]" (платформа для интеграции)
  - "Client: [CLIENT_COMPANY_C]" (клиент банка)
  - "Firm: [STARTUP_D]" (стартап/операционная компания)
- Используйте эту классификацию когда:
  - Создаете/обновляете профили людей с принадлежностью к компаниям
  - Перемещаете компании в папку Companies/
  - Документируете, где люди работают и тип взаимоотношений с банком

---

## Контекст коммуникации

<!-- Telegram группа команды пока не используется
**Основной чат команды фандрайзинга**: Telegram группа "[TEAM_CHAT_NAME]"

- **Участники**: [OWNER_NAME] [владелец], [ASSISTANT_A] [ассистент], [ADMIN_NAME] [админ], [TEAM_MEMBER_B], [TEAM_MEMBER_C], [TEAM_MEMBER_D], [TEAM_MEMBER_E], [TEAM_MEMBER_F]
- **По умолчанию**: Когда упоминается "чат" → относится к этой Telegram группе
- **По умолчанию**: Когда "[OWNER_NAME] написал" → означает в этой Telegram группе
-->

---

## Ключевые файлы и расположения

**Информация о компании**:

- **Blurb** (текущий): `../Strategy/Pitch-Materials/2025 - seed blurb.md` [CANONICAL]
  - Blurb находится в папке Strategy вместе со всеми питч-материалами

  - Обновляйте blurb там, а не в папке CRM

**Управление контактами**:

- **Активные контакты**: `MainCRM/Context/Contacts_Actual.md` [70KB] [ЕДИНСТВЕННЫЙ ИСТОЧНИК ПРАВДЫ для текущего пайплайна]
- **Будущие контакты**: `MainCRM/Context/Contacts_Later.md` [Для контактов, к которым нужно вернуться при достижении вех]
- **Профили контактов**: папки `MainCRM/Contacts/[Name]/`

---

## Структура папок и организация файлов

**Основная рабочая папка**: `MainCRM/`

### Структура директорий:

- `/Contacts/` - Папки контактов с профилями, email, сообщениями Telegram, транскриптами встреч
- `/Companies/` - Профили компаний (партнеры, клиенты, технологические платформы)

- `/Scripts/` - Скрипты автоматизации для обработки данных
- `/Context/` - Ключевые справочные файлы для работы с партнерами и клиентами
- `/Specs/` - Техническая документация для скриптов (документация системы Telegram и т.д.)

### Назначение папки Companies:

**`MainCRM/Companies/`** - Профили компаний/организаций для быстрого поиска контекста

**Назначение**: Найти все релевантные контакты из компании (партнерская компания, клиент, технологическая платформа и т.д.)

**Структура**:

- У каждой компании своя папка: `Companies/[Company Name]/`
- Основной файл профиля: `[Company Name].md`

**Содержимое профиля** (УЛЬТРА МИНИМАЛЬНОЕ):

- **Заголовок**: Название компании, тип, веб-сайт, фокус (по одной строке каждый)
- **Обзор** (ОПЦИОНАЛЬНО): МАКСИМУМ ОДИН абзац о самой компании (тип партнерства/клиентства, ключевые факты, рыночная позиция)
  - О КОМПАНИИ, НЕ о людях
  - Максимум один абзац
- **Секция контактов**: ТОЛЬКО список людей
  - Формат для каждого контакта: `**Name** - Роль, краткий контекст` (одна строка на человека)
  - Пример: `**Jennifer Richard** - Principal, passed Oct 2025 "space too crowded"`
  - БЕЗ email, БЕЗ телефонов, БЕЗ деталей статуса, БЕЗ ссылок на профили
  - Все детали находятся в индивидуальных профилях контактов
- **Дата последнего обновления**

**НИКОГДА не включайте в профили компаний**:
- ❌ История коммуникаций (встречи, email, звонки)
- ❌ Временная линия или история отношений
- ❌ Детали проектов или списки задач
- ❌ Детальные заметки о взаимодействии
- ❌ ЛЮБАЯ информация о конкретных разговорах

**Почему ультра минимально**: Профили компаний существуют ТОЛЬКО для того, чтобы видеть, кого мы знаем в компании, и базовый контекст компании. Все детали контактов и история коммуникаций находятся в индивидуальных профилях контактов.

### Подход к скриптам и документации:

- **Расположение скриптов**: `MainCRM/Scripts/` (текущие), `MainCRM/Scripts/Old/` (устаревшие)
- **Инструкции по использованию**: Написаны непосредственно в этом `.cursorrules` - должно быть достаточно для запуска скриптов без чтения подробной документации
- **Подробная документация**: Только в `MainCRM/Specs/` для понимания внутренних механизмов или изменения скриптов
- **Принципы документации**: Те же, что и в папке Strategy - избегать дублирования, писать кратко, высокая плотность информации. Если это не существенно, не пишите.

### Критические файлы в MainCRM/:

**`Context/Contacts_Actual.md`** [70KB] [БЭКЛОГ для активных контактов]

- Статус отношений и контекст
- Что было обсуждено/согласовано с каждым контактом
- Обязательства и последующие действия (бэклог - ссылка для создания рабочих задач)
- ТОЛЬКО для контактов, с которыми мы активно работаем СЕЙЧАС

**НОВОЕ:** Активные рабочие элементы, извлеченные из этого бэклога, переходят в `Tasks/work_tasks.md` [14KB]

- **КРИТИЧЕСКИ ВАЖНО**: Добавляйте только те элементы, которые были явно обсуждены с контактом ИЛИ явно запрошены пользователем. НЕ выдумывайте задачи по подготовке, исследовательские задачи или "очевидные следующие шаги", если это не было специально запрошено.

**`Context/Contacts_Later.md`** [Для будущих возможностей отношений]

- Контакты, время для которых не подходит СЕЙЧАС, но к которым стоит вернуться позже
- Каждая запись имеет условие "Вернуться, когда" (веха/триггер)
- НЕ является частью активного пайплайна - не требуется немедленных последующих действий
- Примеры: "Вернуться, когда ARR > $XM", "Вернуться, когда Series A", "Вернуться, когда выход на [Рынок]"
- Тот же формат, что и Contacts_Actual.md [70KB], но сфокусирован на будущем времени

**`CRM-Tasks.md`** (на уровне репозитория)

- Общие задачи проекта CRM/работы с партнерами и клиентами
- НЕ для задач, специфичных для контактов

---

## Правила профиля контакта

### ⚠️ КРИТИЧЕСКИ ВАЖНО: Правило управления задачами

**АБСОЛЮТНОЕ ПРАВИЛО: НЕ ВЫДУМЫВАЙТЕ ЗАДАЧИ**

Добавляйте только те задачи, которые:

1. **Явно обсуждались с контактом** (они сказали, что сделают X, или попросили нас сделать Y)
2. **Явно запрошены пользователем** в его запросе

**НИКОГДА не добавляйте следующие**:

- ❌ Задачи по подготовке ("исследовать их фон", "подготовить дек", "проверить LinkedIn")
- ❌ "Очевидные следующие шаги" ("связаться после встречи", "отправить благодарность")
- ❌ Исследовательские задачи, если они не были специально запрошены
- ❌ Задачи "Когда возвращаться" в Contacts_Later.md
- ❌ Любые задачи, которые пользователь не упомянул

**Примеры**:

- ✅ Пользователь говорит "встреча 30 октября" → Добавить только: `[ ] Встреча 30 октября`
- ❌ НЕ добавляйте: "Подготовиться к встрече", "Исследовать компанию", "Связаться после"

**Это относится к**: Contacts_Actual.md [70KB], Contacts_Later.md и всем профилям контактов.

**Правило отказа в исследовании**: Если исследование не удается (веб-поиск, LinkedIn и т.д.), спросите пользователя, что делать: (а) добавить задачи исследования в бэклог контакта в Contacts_Actual.md, (б) попробовать альтернативный метод, или (в) пропустить. НИКОГДА не притворяйтесь, что исследование завершено.

**Стандартный поток нового контакта**: При создании новых профилей контактов (исследование, LinkedIn, синхронизация email) также добавляйте их в Contacts_Actual.md [70KB] с соответствующим контекстом и статусом.

### Contacts_Actual vs Contacts_Later vs Work Tasks:

**Уточнение назначения:**

**`Contacts_Actual.md`** [70KB] = БЭКЛОГ
- Что было согласовано/обсуждено с людьми
- Статус отношений и история
- Контекст для работы с этим контактом
- НЕ ваш непосредственный список дел

**`Contacts_Later.md`** = БУДУЩИЙ БЭКЛОГ
- Контакты, к которым нужно вернуться при достижении вехи
- Ситуации типа "Вернуться, когда ARR > $XM"

**`Tasks/work_tasks.md`** [14KB] = АКТУАЛЬНЫЙ СПИСОК ДЕЛ (НОВОЕ)
- Над чем ВАМ нужно работать НА ЭТОЙ НЕДЕЛЕ
- Организовано по приоритету (P0/P1/P2/P3)
- Агент помогает их выполнять
- См. `MainCRM/Inbox/inbox_processing_rules.md` для рабочего процесса

**Когда использовать каждый:**

**Contacts_Actual** [70KB] - Добавлять, когда:
- Активно общаетесь с контактом
- Часть текущего пайплайна работы с партнерами/клиентами
- Нужно отслеживать статус отношений

**Contacts_Later** - Добавлять, когда:
- Нужно вернуться, когда достигнута веха
- Время не подходит СЕЙЧАС

**Work Tasks** - Добавлять, когда:
- ВАМ нужно что-то сделать НА ЭТОЙ НЕДЕЛЕ/МЕСЯЦ
- Требуется решение или действие
- Подмножество, извлеченное из бэклога Contacts_Actual [70KB]

**Ключевое различие**:
- Contacts_Actual [70KB]/Later = Контекст и бэклог (справка)
- Work Tasks = Активная работа (выполнение)

### Что куда идет:

**Файлы профилей контактов** (`Contacts/[Name]/[Name].md`) содержат ТОЛЬКО:

- Контактную информацию и каналы связи (ФАКТЫ)
- Профессиональный опыт и биографию (ФАКТЫ)
- Текущие проекты и роли (ФАКТЫ)
- Сводки истории коммуникаций (ФАКТЫ - что произошло, а не что делать)
- **Отношения с Alatau City Bank** (ФАКТЫ - как связаны, почему релевантны)
- **НЕТ пунктов действий, задач или следующих шагов**
- **НЕТ анализа, предложений или стратегических соображений**
- **НЕТ "Возможность встречи", "Следующие шаги по обнаружению", "Потенциальные темы для обсуждения"**
- **НЕТ "Стратегической ценности", "Заметок для [Владельца]" или оценок приоритета**

**`Context/Contacts_Actual.md`** [70KB] содержит (БЭКЛОГ):

- Статус отношений и контекст
- Что было обсуждено/согласовано
- Обязательства и следующие шаги (как справка)

**`Tasks/work_tasks.md`** [14KB] содержит (АКТИВНАЯ РАБОТА):

- Подмножество элементов из Contacts_Actual [70KB], которые требуют работы НА ЭТОЙ НЕДЕЛЕ
- Прямые задачи, которые добавляет пользователь
- Организовано по приоритету (P0/P1/P2/P3)

**Правило архивации**: Когда Contacts_Actual.md становится слишком большим, старые/завершенные элементы могут быть перемещены в профили как историческая запись с `[АРХИВИРОВАНО из Contacts_Actual.md: дата]`. Никогда не дублируйте между файлами.

### Рабочий процесс синхронизации:

**НОВОЕ (ноябрь 2025):** Обработка теперь использует трехэтапную систему. См. `MainCRM/Inbox/inbox_processing_rules.md`

**Краткая версия:**
1. Добавить факты коммуникации в профиль
2. Обновить статус отношений в `Contacts_Actual.md` [70KB]
3. Добавить рабочие элементы в `Tasks/work_tasks.md` [14KB] (не в Contacts_Actual)
4. Держать Contacts_Actual [70KB] только как бэклог/справку

---

## Инструменты исследования (Perplexity API)

**Расположение**: `MainCRM/Scripts/ResearchTools/`  
**Техническая документация**: `MainCRM/Scripts/ResearchTools/RESEARCH_TOOLS.md`  
**Конфигурация API**: `SharedWorkspace/Personal/Secrets/credentials.yaml`

### Быстрые команды

**Исследование контактов** (Люди):
- Естественный язык: `"do web research for [Person Name]"`
- CLI: `python3 contact_research.py --name "Name" --email "email" --linkedin "slug" --company "Company" --context "extra details" --save-folder "../../Contacts/Name"`
- Выход: `Contacts/{Name}/{Name}_research.md` + автоматическое обновление профиля

**Исследование компаний** (Организации):
- Естественный язык: `"do company research for [Company Name]"`
- CLI: `python3 company_research.py --name "Company" --type firm --website "domain.com" --context "key people, size" --save-folder "../../../Companies/Firm: Company"`
- Выход: `Companies/{Type}: {Name}/{Name}_research.md` + автоматическое обновление профиля

**Общее исследование** (Темы):
- Естественный язык: `"research: [question]"`
- CLI: `python3 general_research.py --query "Question?" --auto-save`
- Выход: `Research/{Topic}_research.md`

**Устранение неоднозначности**: Используйте флаг `--context` с дополнительными деталями (ключевые люди, местоположение, предыдущие работодатели, отличительные факты), когда имя распространенное или существует несколько сущностей.

### Исследование LinkedIn

**Назначение**: Автоматизированное сканирование профиля LinkedIn с автоматическим обновлением профиля.

**Как запросить** (естественный язык):
- "do linkedin research for [Person Name]"
- "get [Person Name]'s LinkedIn profile"

**Что происходит автоматически**:
1. ✅ **Интеллектуальное обнаружение slug**: Ищет LinkedIn URL во ВСЕХ файлах в папке контакта
   - Основной файл контакта (Name.md)
   - Существующий файл linkedin.md
   - Файл исследования (*_research.md)
   - Файл email (*emails.md)
   - Любые другие .md файлы
   - Использует точный slug, если найден ГДЕ-ЛИБО в данных контакта
   - Только возвращается к поиску по имени, если slug не найден
2. ✅ Сканирует профиль LinkedIn (требуется сессия входа)
3. ✅ Сохраняет в `Contacts/{Name}/{Name} linkedin.md` (ДОСЛОВНОЕ содержимое LinkedIn)
4. ✅ **Создает основной файл контакта** `Contacts/{Name}/{Name}.md`, если он не существует
   - Извлекает ключевую информацию: Имя, Заголовок, Местоположение, Текущая роль
   - Создает обогащенную сводку из раздела "О себе" LinkedIn
   - Добавляет раздел "Профессиональный фон" с текущей позицией
   - Включает примечание об источнике: "Исследование LinkedIn проведено [дата]"
5. ✅ **Обновляет существующий файл контакта**, если он уже существует
   - Добавляет Профессиональную сводку из раздела "О себе" LinkedIn (если еще не присутствует)
   - Обновляет даты "Последнее обновление" и проверки CANONICAL
   - Использует семантическое слияние (сохраняет существующее содержимое)

**Расположение скрипта**: `MainCRM/Scripts/LinkedinDev/linkedin_research.py`

**Ручное использование** (если нужно):
```bash
cd MainCRM/Scripts/LinkedinDev
python3 linkedin_research.py --contact-folder "../../Contacts/John Doe"
```

**Ключевые улучшения**: 
- Флаг `--contact-folder` теперь интеллектуально ищет LinkedIn URL во ВСЕХ файлах в папке контакта, гарантируя, что мы всегда используем правильный, проверенный slug, когда он существует где-либо в данных контакта (linkedin.md, emails, файлы исследований и т.д.). Это избегает ненужных поисков по имени и обеспечивает точность данных.
- **Отслеживание рекомендаций**: Полученные и данные рекомендации теперь извлекаются отдельно из соответствующих вкладок, обеспечивая точное различие. Скрейпер обнаруживает активную панель вкладок и извлекает только из видимой области содержимого.
- **Извлечение навыков**: Теперь следует правильному паттерну навигации по странице деталей - нажимает "Показать все навыки", переходит на страницу деталей, извлекает чистые названия навыков (без количества одобрений, без текста "Одобрить") и возвращается к профилю.

**Документация**:
- `MainCRM/Scripts/LinkedinDev/QUICK_REFERENCE.md` - Руководство по использованию
- `MainCRM/Scripts/LinkedinDev/AUTO_UPDATE_README.md` - Детали автоматического обновления

---

## Каналы коммуникации и скрипты

### Сообщения Telegram

**Основной список**: `MainCRM/Context/sync_list_telegram.txt`

**⚠️ КРИТИЧЕСКИ ВАЖНО: Интерактивный рабочий процесс - НИКОГДА не сохраняйте автоматически без подтверждения**

**Добавление новых контактов**:

**Случай 1: Пользователь предоставляет Telegram handle напрямую** (например, "add @contact_handle"):

1. **Получить user_id**: Поиск с предоставленным handle

   ```bash
   cd MainCRM/Scripts && source venv/bin/activate
   python3 telegram_find_handles.py --person "Name" --search --query "handleOrName"
   # Возвращает JSON с user_id: {"user_id": 12345678, "username": "@handle", ...}
   ```

2. **Сохранить с user_id**: Использовать user_id из результатов поиска

   ```bash

   python3 telegram_find_handles.py --person "Name" --save "@handle" --user-id 12345678
   ```

**Первая синхронизация**: Скачать все сообщения

   ```bash
   python3 telegram_sync.py --person "Name"
   ```

**Случай 2: Нужно найти handle** (пользователь не предоставляет handle):

1. **Поиск**: Запустить скрипт поиска, показать результаты с user_id пользователю

   ```bash
   cd MainCRM/Scripts && source venv/bin/activate

   python3 telegram_find_handles.py --person "Name" --search --query "SearchTerm"
   # Возвращает JSON с user_id: {"user_id": 12345678, "username": "@handle", ...}
   ```

2. **СПРОСИТЬ ПОЛЬЗОВАТЕЛЯ о подтверждении**: Представить результаты, спросить:
   - "Найден [Name] как [@handle] с user_id [12345678]. Это правильно?"
   - "Должно быть #DO_SYNC или #DO_NOT_SYNC?"
   - Дождаться ответа пользователя
3. **ТОЛЬКО ПОСЛЕ подтверждения**: Сохранить handle С user_id

   ```bash
   python3 telegram_find_handles.py --person "Name" --save "@handle" --user-id 12345678
   ```

4. **Первая синхронизация**: Скачать все сообщения

   ```bash
   python3 telegram_sync.py --person "Name"
   ```

**Если не найден**: Спросить пользователя о статусе и сохранить:

- `#NOT_ON_TELEGRAM` - человек не использует Telegram

- `#NOT_CONNECTED` - в Telegram, но еще не в ваших контактах

**⚠️ КРИТИЧЕСКИ ВАЖНО**: ВСЕГДА включайте --user-id при сохранении! Без него синхронизация будет в 3 раза медленнее или не удастся.

**НИКОГДА**:

- ❌ Автоматически сохранять handles без запроса (кроме Случая 1, где пользователь предоставил handle)
- ❌ Угадывать, если совпадение неопределенное

**Советы по поиску** (руководство для AI):

- **Приоритет русских имен**: Для русских имен (например, Дмитрий, Алексей, распространенные фамилии), искать сначала на русском (Дмитрий, Алексей), затем на английском, если не найдено
- **Стратегия запроса**: Фамилия сначала (менее распространенная) → имя → уменьшительное → название компании

- **Варианты имен**: Оба написания (разные транслитерации), уменьшительные (Alexander→Alex/Sasha, Dmitry→Dima), транслитерации (Dmitry/Dmitri/Dmitriy)
- **Цель предпросмотра**: Проверить через контекст сообщений (упоминания компании, общие связи, темы), номер телефона, недавняя активность
- **Несколько аккаунтов**: Выбрать активный (недавние сообщения), отметить вторичный в профиле
- **Не найден**: Попробовать 2-3 варианта поиска (Русский → Английский → уменьшительное), затем спросить пользователя

**Регулярные операции**:

```bash
cd MainCRM/Scripts && source venv/bin/activate

# Синхронизировать один контакт (инкрементально - только новые сообщения)
python3 telegram_sync.py --person "Name"

# Перезагрузить контакт с конкретной даты (перезаписать период)
python3 telegram_sync.py --person "Name" --since "2025-10-01"

# Синхронизировать все контакты #DO_SYNC (еженедельная пакетная)
python3 telegram_sync.py --all

```

**Режимы синхронизации**:

- **Первая синхронизация**: Нет временной метки → скачивает все сообщения
- **Инкрементальная**: Использует `[DONE:timestamp]` → добавляет только новые сообщения (по умолчанию)
- **Перезагрузка**: Использует `--since DATE` → перезаписывает сообщения с даты (сохраняет более старые)

**Теги**: `#DO_SYNC` (бизнес), `#DO_NOT_SYNC` (личное), `#NOT_ON_TELEGRAM` (не на платформе), `#NOT_CONNECTED` (на платформе, не подключен)

**Документация**: См. `MainCRM/Specs/TELEGRAM_SYSTEM_README.md` для полной документации.

---

### Синхронизация Telegram Inbox (AI Inbox)

**Назначение**: Синхронизация сообщений из группы Telegram "[AI_INBOX_NAME]", куда пользователь отправляет заметки себе. (Пока не используется)

**Расположение**: Сообщения сохраняются в `MainCRM/Inbox/telegram ai inbox.md`

**Быстрые команды**:

```bash
cd MainCRM/Scripts
source venv/bin/activate

# Инкрементальная синхронизация (рекомендуется - получить новые сообщения с последней синхронизации)
python3 telegram_inbox_sync.py

# Перезагрузить все сообщения с начала
python3 telegram_inbox_sync.py --reload

# Перезагрузить с конкретной даты/времени (поддерживается несколько форматов)
python3 telegram_inbox_sync.py --since "2025-10-21"              # Только дата (00:00:00)
python3 telegram_inbox_sync.py --since "2025-10-21 14:30"        # Дата + время
python3 telegram_inbox_sync.py --since "2025-10-21T14:30:00"     # Формат ISO
```

**Случай использования**: Идеально для нескольких синхронизаций в день. Используйте инкрементальный режим для регулярного рабочего процесса - он автоматически отслеживает последнюю синхронизацию и скачивает только новые сообщения.

---

### Расшифровка голосовых сообщений Telegram (OpenAI Whisper)

**Назначение**: Расшифровать голосовые сообщения, когда нативная расшифровка Telegram не удается (ошибка MSG_VOICE_TOO_LONG для сообщений >5 минут).

**Расположение**: `MainCRM/Scripts/telegram_transcribe_voice.py`

**Требования**:
- Ключ API OpenAI хранится в `Personal/Secrets/credentials.yaml` (уже настроен)
- ffmpeg установлен: `brew install ffmpeg`
- Пакет openai: `pip install openai`

**Быстрые команды**:

```bash
cd MainCRM/Scripts

# Расшифровать последнее голосовое сообщение из AI inbox
python3 telegram_transcribe_voice.py

# Расшифровать конкретное сообщение по ID
python3 telegram_transcribe_voice.py --message-id 353474

# Сохранить в файл вместо stdout
python3 telegram_transcribe_voice.py --output transcription.txt
```

**Как это работает**:
1. Загружает ключ API OpenAI из `Personal/Secrets/credentials.yaml` (или переменную окружения OPENAI_API_KEY)
2. Скачивает голосовое сообщение из Telegram (формат OGG)
3. Конвертирует OGG → WAV используя ffmpeg (16kHz, моно)
4. Расшифровывает используя OpenAI Whisper API (отлично обрабатывает переключение RU/EN)
5. Выводит расшифровку в терминал или файл

**Случай использования**: Когда синхронизация inbox показывает `🎤 [Голосовое сообщение]` без расшифровки из-за ошибки MSG_VOICE_TOO_LONG, используйте этот скрипт для локальной расшифровки.

---

### Рабочий процесс обработки AI Inbox

**ВАЖНО:** Обработка inbox теперь использует трехэтапную систему с контрольными точками.

**Для полного рабочего процесса, прочитайте:**
```
MainCRM/Inbox/inbox_processing_rules.md
```

**Краткая сводка:**
- **Этап 1:** Inbox → Задачи обработки (КОНТРОЛЬНАЯ ТОЧКА перед архивированием)
- **Этап 2:** Задачи обработки → Рабочие задачи (КОНТРОЛЬНАЯ ТОЧКА перед очисткой)
- **Этап 3:** Рабочие задачи → Выполнение (агент помогает с задачами)

**Ключевые файлы:**
- `MainCRM/Inbox/processing_tasks.md` - Временный (сбор информации)
- `MainCRM/Tasks/work_tasks.md` [14KB] - Постоянный (активный список дел пользователя)
- `MainCRM/Context/Contacts_Actual.md` [70KB] - Бэклог (соглашения с людьми)

**См. inbox_processing_rules.md для подробного рабочего процесса и процедур контрольных точек.**

---

### Синхронизация Email (ПО УМОЛЧАНИЮ: Новая система Gmail)

**⚠️ ВАЖНО**: Когда пользователь просит "обновить email" или "синхронизировать email", используйте **НОВУЮ систему Gmail** по умолчанию.

**НОВАЯ система Gmail** (октябрь 2025) - **ПО УМОЛЧАНИЮ ДЛЯ ВСЕХ ОПЕРАЦИЙ С EMAIL**

**Расположение**: `MainCRM/Scripts/Gmail/`

**Основной список**: `MainCRM/Context/sync_list_gmail.txt`

**Использование**:

```bash
cd MainCRM/Scripts/Gmail


# Найти email для контакта
python3 gmail_find_email.py --person "Name" --search
python3 gmail_find_email.py --person "Name" --save "email@example.com"

# Синхронизировать один контакт (инкрементально)
python3 gmail_sync.py --person "Name"

# Перезагрузить с конкретной даты
python3 gmail_sync.py --person "Name" --since "2025-10-01"

# Синхронизировать все контакты #DO_SYNC (пакетно)
python3 gmail_sync.py --all

# Сгенерировать отчет о статусе
python3 gmail_analyze_status.py
```

**Рабочий процесс после синхронизации** (КРИТИЧЕСКИ ВАЖНО):

После завершения `gmail_sync.py` это происходит **автоматически**:

1. ✅ Файл Email MD создан/обновлен: `Contacts/[Name]/[Name] emails.md`
2. ✅ Список синхронизации обновлен: Временная метка в `sync_list_gmail.txt`

После синхронизации это происходит **автоматически** (НОВОЕ - октябрь 2025):
3. ✅ **Профиль контакта обновлен**: Добавляет/обновляет раздел "Email коммуникация" с:

- Количеством email и диапазоном дат
- Обнаруженными ключевыми темами (из тем)
- Ссылкой на файл email

4. ✅ **Обнаружены пункты действий**: Сканирует недавние email на предмет потенциальных задач из паттернов типа "Я сделаю X", "Можешь сделать Y?"
5. ✅ **Contacts_Actual.md** [70KB] **обновлен** (если контакт является активным партнером/клиентом):
   - Обновляет дату "Последний контакт"
   - Добавляет HTML комментарий, если обнаружены пункты действий

**Требуется ручной просмотр**:

- Просмотреть обнаруженные пункты действий и добавить конкретные задачи в Contacts_Actual.md
- Проверить точность автоматически сгенерированной сводки email
- Обновить статус/контекст на основе содержимого email

**Пример обновления профиля**:

```markdown
## Email коммуникация
[CANONICAL: Gmail скачивание 21 октября 2025]

**Сводка email коммуникации** (27 писем, август 2022 - ноябрь 2023):

Обсуждения партнерства, интеграции, интро по найму, обмен контактами партнеров.

**Полная история email**: [Name emails.md](./Name%20emails.md)
```

**Теги**: `#DO_SYNC` (бизнес), `#DO_NOT_SYNC` (личное), `#NO_EMAIL` (не найден), `#BOUNCED` (недействительный)

**Документация**: См. `[Legacy_Folder_A]/Gmail_temp/` для плана реализации и полной документации.

---

### Устаревшая система Gmail (УСТАРЕВШАЯ - Использовать только при явном запросе)

**⚠️ УСТАРЕВШАЯ**: Старый скрипт `gmail_downloader_api.py` устарел с октября 2025. **Используйте новую систему Gmail выше по умолчанию.**

**Расположение**: `MainCRM/Scripts/gmail_downloader_api.py`

**Используйте эту устаревшую систему только когда**:

- Пользователь явно просит "устаревшую систему Gmail" или "старый Gmail downloader"
- Пользователь конкретно упоминает `gmail_downloader_api.py`
- Пользователь просит использовать "старую систему email"

**Использование** (устаревшее):

```bash
cd MainCRM/Scripts
python3 gmail_downloader_api.py --person "Full Name"
```

**Известная проблема**: Первый запуск может выдать ошибку с `persisted_addresses` - просто запустите дважды.

**Почему устарела**:

- Нет автоматического обновления профилей
- Нет обнаружения пунктов действий
- Нет интеграции с Contacts_Actual.md [70KB]
- Нет возможности инкрементальной синхронизации
- Требует ручной обработки данных email

**Миграция**: Все контакты должны использовать новую систему Gmail (`Scripts/Gmail/`) для синхронизации email.

---

## Стандарты коммуникации

**Важно**: Реальные стандарты коммуникации (время, ожидания ответа, правила приоритета) находятся в этом файле `.cursorrules`. Если стандарт достаточно важен для соблюдения, он принадлежит здесь. Если он не важен, не записывайте его.

**Каналы коммуникации**: Предпочтения контактов и наши предпочтения для каналов коммуникации документируются в профилях контактов (см. шаблон ниже), НЕ в Contacts_Actual.md [70KB].

---

## Руководящие принципы стиля коммуникации

**При составлении сообщений (любого типа, любого языка) используйте этот ультра-краткий стиль:**

**Основные характеристики**:

- **Максимальное сжатие**: Только существенные факты, ноль лишнего
- **Высокая плотность информации**: Каждое слово должно нести вес
- **Числа и факты**: Названия компаний, выручка, финансирование, рейтинги, местоположения
- **Выборочные числа**: Только 2-3 самых релевантных числа на email (например, $XM расходы на рекламу, $YM ARR) - не полные дампы метрик
- **Ноль прилагательных или маркетингового языка**: Нет "amazing", "great", "wonderful"
- **Чистые факты**: Пусть достижения говорят сами за себя
- **Тепло, но профессионально**: Дружелюбно без цветистости
- **Уверенно и прямо**: Без оговорок, без излишних объяснений
- **Короткие абзацы**: Максимум 2-4 предложения на абзац

**Чего НЕ включать**:

- ❌ Подробные фоновые истории
- ❌ Язык типа "I hope this is valuable" / "I think this could be helpful"
- ❌ Эмоциональные комментарии или реакции
- ❌ Тяжелое форматирование или отдельные секции
- ❌ Подробные объяснения связей
- ❌ Множество причин, нагроможденных вместе
- ❌ Слишком много метрик (ограничьтесь только 2-3 самыми влиятельными числами)

**Формулировка запросов**:

- Приуменьшайте запросы: "локальный запрос" (русский) или случайная формулировка (английский)
- Начинайте с более широкой ценности, а не с запроса
- Добавляйте практический контекст (география, время, взаимная выгода)
- Без давления, без продаж

**Основной принцип**: Высокая плотность фактов, минимум слов, пусть люди сами соединят точки.

---

**Example 1 - Russian Introduction**:

```
Всем привет!

Хочу вас познакомить.

[Person A] - фаундер [Company Alpha], до этого [Company Beta], сейчас партнер и board member в [Company Gamma], развивает новые проекты, живёт в [City A].

[Person B] и [Person C] - ко-фаундеры [Company Delta] (топ-платформа в [Region], подняли $XM+, выручка $YM+ в год). Сейчас они развивают [Product Epsilon] в [Region 2]. Партнеры Alatau City Bank по интеграции [Product/Service]. Живут в [City B].

У [Person A] есть локальный запрос про поиск [Role] в стартап, но мне кажется вам в принципе будет интересно познакомиться, тем более что вы живёте почти рядом.
```

**Example 2 - English Introduction**:

```
[To be filled with actual example when available]
```

---

---

## Стандарты документации

Следуйте тем же принципам, что и репозиторий Strategy ([REF: ../Strategy/.cursorrules](../Strategy/.cursorrules)):

**Отслеживание источников**:

- `[CANONICAL]` - Первое/авторитетное упоминание
- `[ПОДТВЕРЖДЕНО: источник]` - Проверено из конкретного источника
- `[REF: path/file.md#section]` - Перекрестная ссылка
- `[PLACEHOLDER: владелец]` - Отсутствующая информация с указанием владельца

**Ключевые принципы** (адаптированы для контекста CRM):

- Каждое утверждение требует отслеживаемого источника
- Отмечайте, откуда пришла информация (встреча, email, LinkedIn и т.д.)
- Датируйте все обновления
- Нет выдуманного содержимого
- Качество > количество

---

## Шаблон профиля контакта

### ⚠️ КРИТИЧЕСКИ ВАЖНО: ПРОФИЛИ - ТОЛЬКО ФАКТЫ

**АБСОЛЮТНОЕ ПРАВИЛО**: Профили контактов содержат ТОЛЬКО фактическую информацию. НЕТ анализа, предложений, планов или стратегических соображений.

**Профили содержат ТОЛЬКО**:

- ✅ Контактную информацию и каналы коммуникации
- ✅ Профессиональный фон (проверенные факты)
- ✅ Историю коммуникаций (что реально произошло)
- ✅ Отношения с Elly (фактическая связь, как мы встретились)
- ✅ Ссылку на профиль компании (если применимо) - для полного контекста команды

**НИКОГДА не включайте в профили**:

- ❌ Пункты действий или списки задач
- ❌ Секции "Следующие шаги"
- ❌ Напоминания о последующих действиях или предложения
- ❌ "Задачи от контакта" или "Задачи с нашей стороны"
- ❌ Любые элементы с чекбоксами [ ]
- ❌ Секции "Возможность встречи"
- ❌ "Следующие шаги по обнаружению"
- ❌ "Потенциальные темы для обсуждения"
- ❌ "Стратегическая ценность" или "Стратегические соображения"
- ❌ "Заметки для [Владельца]" с анализом/предложениями
- ❌ Уровни приоритета, предложения по времени или рекомендации
- ❌ ЛЮБЫЕ предложения, предположения или планы
- ❌ Информация о ДРУГИХ людях из той же компании (роли, обязанности, кто является основным контактом и т.д.)

**Роли в компании и структура команды**:

- ✅ Записывайте в профиле компании: роли всех людей, основные контакты, иерархия команды
- ✅ Записывайте в профиле человека: ТОЛЬКО об этом конкретном человеке
- ✅ Добавьте ссылку на профиль компании для полного контекста команды

**Задачи и пункты действий относятся к:**

- `Context/Contacts_Actual.md` [70KB] - Бэклог (что было обсуждено/согласовано с контактом)
- `Context/Contacts_Later.md` - Будущий бэклог (вернуться, когда достигнута веха)
- `Tasks/work_tasks.md` [14KB] - Активная работа (что делать НА ЭТОЙ НЕДЕЛЕ)

**Профили являются фактическими справочными документами**, а не документами стратегического планирования или задач.

### Базовая структура:

```markdown
# [Полное имя] - Профиль

**Роль**: [Должность] в [Компания] [CANONICAL]
**Дата контакта**: [Дата] ([Метод]) [CANONICAL]
**Контекст**: [Как связаны] [ПОДТВЕРЖДЕНО: источник]

**Каналы связи**:
- **Email**: [email] [CANONICAL] - [Предпочтение контакта / наша оценка]
- **Телефон**: [номер] или [PLACEHOLDER] - [Предпочтение контакта]
- **LinkedIn**: [URL] [CANONICAL - Verified YYYY-MM-DD] - [Предпочтение контакта]
- **Telegram**: [@handle] [если применимо] - [Предпочтение контакта]
- **Текстовые сообщения**: [если применимо] - [Предпочтение контакта]
- **Предпочтительный канал**: [Какой канал мы считаем наиболее эффективным для этого контакта]

Примечание: Укажите отношение контакта к каждому каналу и какой мы предпочитаем использовать.

---

## Правила формата Contacts_Actual.md [70KB]

**Строка заголовка** (после ## Имя): МАКСИМУМ ОДИН абзац. Супер сжатые ключевые факты - как теги/ключевые слова. Только самое необходимое для распознавания контакта: роль, компания, специализация, как связаны. Плотный стиль с маркерами, НЕ полные предложения или подробные описания. Думайте "быстрый поиск", а не "биография".

**Статус в одну строку**: ⚠️ **ВСЕГДА ТОЛЬКО ОДНА СТРОКА** ⚠️ 

Текущее состояние ОДНИМ ПРЕДЛОЖЕНИЕМ. Точка. Примеры:
- "Новый контакт, нужно связаться."
- "Ждем ответа до четверга."
- "Ждем решения Максима о партнерстве/интеграции."
- "Встретились 20 октября, встреча с CMO запланирована на 6 ноября для планирования вебинара, ждем приглашения в сообщество."

**АБСОЛЮТНОЕ ПРАВИЛО**: 
- ❌ НЕТ многоабзацных объяснений статуса
- ❌ НЕТ подробных сводок встреч в статусе
- ❌ НЕТ стратегического анализа в статусе
- ✅ Статус = ОДНО предложение, текущее состояние, и все

**Раздел задач** (БЭКЛОГ - для контекста):
- Используйте "Задачи от [Имя]" и "Задачи с нашей стороны" для документирования того, что было обсуждено/согласовано
- Это СПРАВКА/БЭКЛОГ - активные рабочие элементы переходят в `Tasks/work_tasks.md` [14KB]
- Если конкретные задачи не обсуждались - просто используйте поле Статус, удалите разделы Задач

**Назначение:** Contacts_Actual [70KB] предоставляет контекст при работе над связанными элементами work_tasks.md [14KB]. Это не ваш ежедневный список дел.

---

## Правила формата Contacts_Later.md

**Строка заголовка** (после ## Имя): МАКСИМУМ ОДИН абзац. То же, что и Contacts_Actual.md [70KB].

**Последний контакт**: Дата последнего взаимодействия (хранится в Contacts_Later для исторического контекста)

**Вернуться, когда**: Конкретная веха или условие для повторного обращения к этому контакту

**Статус в одну строку**: Краткая сводка, включая причину ожидания

**АБСОЛЮТНОЕ ПРАВИЛО**: 
- ❌ НЕТ задач "когда возвращаться" в Contacts_Later.md
- ✅ Сосредоточьтесь на условии/вехе, а не на действиях

---

## Профессиональный опыт
[CANONICAL: LinkedIn ГГГГ-ММ-ДД]

### Текущая роль: [Компания] (Начало - Настоящее время)
**Должность**: [Название]
**Местоположение**: [Город, Страна]

**Контекст компании**: [Чем занимается компания]

### [Предыдущие роли]
[Список в обратном хронологическом порядке]

---


## Образование
### [Университет] (Годы)

**Степень**: [Степень] в [Область]

---

## Опыт и достижения
- [Ключевые достижения, награды, примечательные факты]

---

## Отношения с Alatau City Bank

**Почему релевантно**:
1. [Категория]: [Детали]

2. [Категория]: [Детали]

**Потенциальная ценность**:
- [Конкретные интродукции]
- [Консультационный потенциал]
- [Интерес к партнерству/интеграции]
```

**Структура файлов в папке контакта**:

- `[Имя].md` - Профиль (редактировать вручную)
- `[Имя] linkedin.md` - Полное содержимое профиля LinkedIn (редактировать вручную, полные исходные данные)
- `[Имя] emails.md` - История email (генерируется скриптом, НЕ редактировать вручную)
- `[Имя] telegram.md` - Сообщения Telegram (генерируется скриптом, НЕ редактировать вручную)
- `ГГГГ-ММ-ДД - [Название встречи].txt` - Полный транскрипт, если доступен (Fireflies, Zoom и т.д.)
- `ГГГГ-ММ-ДД - Заметки о встрече.md` - Ручные заметки, если транскрипт недоступен

**Правило файлов встреч**: Один файл на встречу - полный транскрипт, если доступен, заметки о встрече только если транскрипт недоступен

**⚠️ КРИТИЧЕСКИ ВАЖНО - НИКОГДА НЕ РЕДАКТИРУЙТЕ АВТОГЕНЕРИРУЕМЫЕ ФАЙЛЫ**:

- `[Имя] emails.md` и `[Имя] telegram.md` АВТОГЕНЕРИРУЮТСЯ скриптами
- Эти файлы заполняются и обновляются ТОЛЬКО скриптами (gmail_downloader_api.py и telegram_sync.py)
- НИКОГДА не редактируйте эти файлы вручную - любые ручные изменения будут перезаписаны при следующей синхронизации
- Если требуются обновления: перезапустите соответствующий скрипт вместо редактирования
- ЕДИНСТВЕННОЕ исключение: Пользователь явно запрашивает игнорировать это правило

**Формат списка синхронизации** (обновлено 2025-10-18):

```
Name -> @handle [ID:user_id] #TAG [DONE:timestamp]

Example:
Contact A -> @contact_a [ID:12345678] #DO_SYNC [DONE:2025-10-18T00:25:00]
```

**⚠️ user_id ОБЯЗАТЕЛЕН** для быстрой синхронизации (в 3 раза быстрее - 6 сек против 20 сек на контакт)

**Формат файла Telegram** (обновлено 2025-10-18):

```
# [Имя]

## История Telegram с [Отображаемое имя] (handle: @username)

2023-01-15T10:30:00+00:00 me: Ваш текст сообщения

2023-01-15T12:45:00+00:00 ←←: Их ответ

2023-01-16T08:00:00+00:00 me: Другое сообщение
```

**Ключевые детали формата**:

- **Порядок сообщений**: Хронологический (самые старые сверху, самые новые снизу) ← ВАЖНО!

- **Ваши сообщения**: Отмечены `me`
- **Их сообщения**: Отмечены `←←` (двойная стрелка влево)
- **Без маркера**: Сообщения начинаются непосредственно с временной метки (без префикса "- ")
- **Пустые строки**: Между каждым сообщением для читаемости
- **Чтобы найти новейшие сообщения**: Прокрутите до КОНЦА файла, а не до начала

**Пример хорошо структурированного контакта**: См. примеры папок контактов в директории `Contacts/`

---

## Система профилей LinkedIn

**Когда пользователь просит обновить LinkedIn или скачать профиль LinkedIn**:

### ⚠️ КРИТИЧЕСКИЕ ПРАВИЛА - ВСЕГДА СЛЕДУЙТЕ:

1. **ВСЕГДА запускайте инструмент скрейпинга LinkedIn** (`linkedin_helper.py`) для получения свежих данных - НИКОГДА не используйте старые кэшированные JSON-файлы
2. **ВСЕГДА следуйте рабочему процессу из `.cursorrules`** - Проверяйте этот раздел перед началом
3. **ВСЕГДА используйте КАНОНИЧЕСКИЙ формат** с датами загрузки: `[CANONICAL - Downloaded YYYY-MM-DD]`
4. **НИКОГДА не полагайтесь на старые кэшированные файлы**, если пользователь явно не запрашивает использование кэшированных данных

**Расположение инструмента**: `MainCRM/Scripts/LinkedinDev/linkedin_helper.py`
**Использование**: `from linkedin_helper import quick_research; data = quick_research("linkedin-slug")`

### Шаг 1: Использование инструмента скрейпинга LinkedIn

1. **Запустите скрейпер LinkedIn**: Используйте `linkedin_helper.py` для получения данных профиля
   ```python
   from linkedin_helper import quick_research, cleanup
   profile_data = quick_research("https://www.linkedin.com/in/[slug]/")
   # или
   profile_data = quick_research("[slug]")  # Работает и просто slug
   cleanup()  # Всегда очищайте после завершения
   ```

2. **Проверьте данные**: Убедитесь, что имя, компания, местоположение, заголовок соответствуют ожидаемому человеку
3. **Если скрейпер не работает**: Вернитесь к инструменту браузера (`browser_navigate` + `browser_snapshot`) для ручного извлечения

### Шаг 2: Сохранение полного содержимого LinkedIn

**Создайте отдельный файл LinkedIn**: `MainCRM/Contacts/[Имя]/[Имя] linkedin.md`

**⚠️ КРИТИЧЕСКИ ВАЖНО: КОПИРУЙТЕ КАК ЕСТЬ - БЕЗ ПЕРЕФРАЗИРОВАНИЯ**

**АБСОЛЮТНОЕ ПРАВИЛО**: Копируйте содержимое LinkedIn ТОЧНО так, как оно отображается - слово в слово, один к одному. НЕ:
- ❌ Перефразируйте или переписывайте ЛЮБОЙ текст
- ❌ Суммируйте или сокращайте разделы
- ❌ "Очищайте" или редактируйте содержимое
- ❌ Переводите или изменяйте язык
- ✅ Копируйте все ДОСЛОВНО из LinkedIn

**Содержимое для захвата**:
```markdown
# [Полное имя] - Профиль LinkedIn

**URL профиля**: [URL] [CANONICAL - Downloaded YYYY-MM-DD]

---

## Содержимое профиля

[Скопируйте ПОЛНОЕ содержимое профиля LinkedIn сюда ТОЧНО так, как оно написано на LinkedIn]

**Заголовок**: [Их заголовок LinkedIn - ТОЧНЫЙ ТЕКСТ]

**О себе**:
[Полный раздел "О себе" - ТОЧНЫЙ ТЕКСТ, скопируйте и вставьте как есть]

**Опыт**:
[Все должности с полными описаниями - ТОЧНЫЙ ТЕКСТ, скопируйте и вставьте как есть]

**Образование**:
[Все записи об образовании - ТОЧНЫЙ ТЕКСТ, скопируйте и вставьте как есть]

**Навыки и рекомендации**:
[Список ключевых навыков - ТОЧНЫЙ ТЕКСТ, скопируйте и вставьте как есть]

**Рекомендации**:
[Любые видимые рекомендации - ТОЧНЫЙ ТЕКСТ, скопируйте и вставьте как есть]

**Дополнительные разделы**:
[Языки, Публикации, Патенты и т.д. - ТОЧНЫЙ ТЕКСТ, скопируйте и вставьте как есть]

---

**Последнее обновление**: [Дата]
```

**Назначение**: Хранить полные исходные данные для будущего использования. Копируйте все ДОСЛОВНО - это канонический источник, а не сводка.

### Шаг 3: Обновление основного профиля с обогащенной сводкой

В `MainCRM/Contacts/[Имя]/[Имя].md` добавьте/обновите раздел "Профессиональный опыт":

**⚠️ ОБОГАЩЕННЫЙ ИНТЕЛЛЕКТ - НЕ ТОЛЬКО LINKEDIN**: Объедините ВСЮ доступную информацию в папке контакта:
- Профиль LinkedIn (из `[Имя] linkedin.md`) - но НЕ копируйте абзацы дословно
- Email-коммуникация (из `[Имя] emails.md`)
- Сообщения Telegram (из `[Имя] telegram.md`)
- Заметки о встречах (из файлов транскриптов встреч)
- Любые другие файлы в папке контакта

**Что включать**:
- ✅ Текущая роль и компания с деталями, полученными из звонков/email
- ✅ Тип компании, услуги, масштаб клиентов, технологический стек (из разговоров)
- ✅ Конкретные детали, НЕ указанные в LinkedIn (например, "$XK/месяц рекламных расходов", "стоимость [Набор инструментов] $YK/квартал")
- ✅ Предыдущие должности и образование
- ✅ Ключевые достижения, специализации, философия
- ✅ Подкасты, сторонние проекты, известные клиенты
- ✅ ВЕСЬ ценный контекст из ваших взаимодействий

**Что НЕ делать**:
- ❌ Копировать раздел "О себе" из LinkedIn дословно в основной профиль (он остается в linkedin.md)
- ❌ Копировать описания опыта из LinkedIn слово в слово (они остаются в linkedin.md)
- ❌ Удалять ценную информацию, полученную из звонков/email

**Добавьте ссылку на полный источник**: `[CANONICAL: LinkedIn загружен ГГГГ-ММ-ДД, Даты звонков/email, см. [Имя] linkedin.md для полного профиля]`

**Ключевой принцип**: Основной профиль = Ваши обогащенные знания о человеке (LinkedIn + разговоры + email). Файл LinkedIn = Необработанные данные LinkedIn.

**Пример**:
```markdown
## Профессиональный опыт
[CANONICAL: LinkedIn загружен 2025-10-29, Звонок 31 октября 2025, Email. См. [Имя] linkedin.md для полного профиля]

### Текущие роли

**CEO & Основатель, Название компании** (Начало - Настоящее время)
- **Местоположение**: [Город, Штат]
- **Тип**: [Тип бизнеса]
- **Услуги**: [Что они делают]
- **Масштаб клиентов**: [Конкретные детали из разговоров]
- **Технологический стек**: [Используемые инструменты + стоимость, если упомянуто]
- **Известные клиенты**: [Если упомянуто]
- **Веб-сайт**: [URL]

### Предыдущие роли:
- **[Компания]** ([Годы]): [Должность] - [Ключевые детали]

## Образование
### [Университет] (Годы)
**Степень**: [Степень] в [Область]
```

### Сводка рабочего процесса:

1. **Поиск** → Найти профиль LinkedIn в браузере или использовать скрипт
2. **Сохранить полное содержимое ДОСЛОВНО** → Создать/обновить `[Имя] linkedin.md` с ТОЧНЫМ текстом LinkedIn (БЕЗ перефразирования)
3. **Создать ОБОГАЩЕННУЮ сводку** → Обновить основной профиль с объединенной информацией из ВСЕХ источников (LinkedIn + звонки + email + и т.д.)
4. **Отметить источник** → Ссылка на [Имя] linkedin.md в основном профиле

**КРИТИЧЕСКИЕ ПРАВИЛА**:
- ✅ Файл LinkedIn = ДОСЛОВНАЯ копия (точный текст из LinkedIn)
- ✅ Основной профиль = ОБОГАЩЕННАЯ информация (LinkedIn + звонки + email + встречи - со всеми ценными деталями)
- ❌ НИКОГДА не копируйте абзацы LinkedIn дословно в основной профиль
- ❌ НИКОГДА не удаляйте ценную информацию из звонков/email при обновлении основного профиля

**Структура файлов в папке контакта**:
- `[Имя].md` - Основной профиль (краткая сводка)
- `[Имя] linkedin.md` - Полное содержимое LinkedIn (полный источник)
- `[Имя] emails.md` - История email (автогенерируется)
- `[Имя] telegram.md` - Сообщения Telegram (автогенерируется)

---

## Проверка LinkedIn (Устаревшая)

**Для быстрой проверки без полной загрузки**:

1. Используйте инструмент браузера: `browser_navigate` + `browser_snapshot`
2. Проверьте: имя, компанию, местоположение, должность соответствуют
3. Отметьте как `[CANONICAL - Verified YYYY-MM-DD]`
4. Если только веб-поиск, отметьте `[TO BE VERIFIED]`

---

## Чек-лист качества профиля

Перед тем как отметить профиль как завершенный:

- [ ] Папка создана с правильным именем
- [ ] Файл email создан и заполнен
- [ ] Файл профиля со всеми разделами
- [ ] Профиль LinkedIn загружен в `[Имя] linkedin.md` (если доступен)
- [ ] LinkedIn проверен в браузере
- [ ] Адрес email найден
- [ ] История коммуникаций задокументирована
- [ ] Отношения с компанией проанализированы
- [ ] Источники задокументированы с датами

---

## Распространенные ошибки, которых следует избегать

- ❌ Не отмечайте LinkedIn `[CANONICAL]` без проверки в браузере
- ❌ Не помещайте пункты действий в профиль (только в Contacts_Actual.md [70KB])
- ❌ Не используйте результаты веб-поиска как канонический источник
- ❌ Не дублируйте информацию между профилем и Contacts_Actual.md [70KB]

- ❌ Не создавайте профили без проверки существующих файлов email/telegram

---

## Инструменты и ресурсы

**Профили LinkedIn**: Всегда используйте инструмент браузера (`browser_navigate` + `browser_snapshot`) для чтения профилей LinkedIn - пользователь авторизован.

## Управление сетью коннекторов

**Назначение**: Отслеживать и управлять интродукциями из нашей сети коннекторов для расширения партнерской сети и привлечения клиентов.

### Управление списком интродукций

**Расположение файла**: `MainCRM/Contacts/[Имя коннектора]/Intro_list.md` - У каждого коннектора есть свой файл отслеживания интродукций в его папке контакта

**Структура**:

- **Индивидуальные файлы**: Каждый коннектор получает свой `Intro_list.md` в своей папке контакта
- **Отслеживание статуса интродукции**: Каждая потенциальная интродукция имеет статус
- **Детали компании/человека**: Отрасль, тип бизнеса, профили LinkedIn, заметки
- **История интродукций**: Отслеживание завершенных интродукций и результатов

**Легенда статусов**:

- **Может представить** - Коннектор подтвердил, что может сделать интродукцию
- **Интро успешно** - Интродукция была сделана и контакт установлен
- **Интро неуспешно** - Интродукция была предпринята, но не удалась
- **Коммуникация в процессе** - В настоящее время обсуждается интродукция
- **Результат достигнут** - Интродукция привела к успешному бизнес-результату
- **Результат не достигнут** - Интродукция сделана, но бизнес-результата нет

### Рабочий процесс коннектора

**При добавлении новых коннекторов**:

1. **Идентифицировать коннектора**: Отметить человека как "Коннектор" в его профиле контакта (`Тип контакта: Коннектор`)
2. **Создать список интродукций**: Создать файл `Intro_list.md` в его папке контакта
3. **Документировать сеть**: Добавить его потенциальные интродукции в его `Intro_list.md`
4. **Отслеживать статус**: Обновлять статус интродукции по мере прогресса
5. **Последующие действия**: Регулярные проверки прогресса интродукций

**При обработке интродукций**:

1. **Запросить интродукцию**: Связаться с коннектором, чтобы запросить конкретную интродукцию
2. **Обновить статус**: Изменить статус с "Может представить" на "Коммуникация в процессе" в его `Intro_list.md`
3. **Отслеживать результат**: Обновить статус на основе успеха/неудачи интродукции
4. **Документировать результаты**: Переместить завершенные интродукции в раздел "История интродукций" с результатами

**Структура шаблона файла**:

```markdown
# [Имя коннектора] - Сеть интродукций

**Коннектор**: [Имя]
**Роль**: [Должность/Компания]
**Тип**: [Описание типа коннектора]
**Статус**: [Статус коннектора]
**Последнее обновление**: [Дата]

## Легенда статусов интродукций
[Определения статусов]

---

## Потенциальные интродукции

| Компания/Человек | Отрасль/Тип бизнеса | Статус | Заметки |
|---|---|---|---|
| **[Имя]** | [Детали] | [Статус] | [Заметки] |

---

## История интродукций

*[Завершенные интродукции и результаты]*

---

## Заметки
- Обновляйте статус по мере запроса и осуществления интродукций
- Документируйте результаты и последующие действия в этом файле
- Перекрестная ссылка с Contacts_Actual.md [70KB] для активных запросов на интродукции
```

**Ключевые коннекторы** (с файлами Intro_list.md):

- **[Коннектор A]** - [Роль] в [Компания A], обширная сеть партнеров и клиентов (документировано N+ связей)
- **[Коннектор B]** - Ключевой коннектор по партнерствам, представил несколько партнерских компаний ([Компания A], другие)
- **[Коннектор C]** - Стратегический коннектор и представитель (основатель [Компания B])
- **[Коннектор D]** - Высокоценный коннектор в [Отрасль] (связи с [Компания C], [Компания D], [Компания E])
- **[Коннектор E]** - Активный коннектор, может предоставить интро в [Компания B]
- **[Коннектор F]** - Коннектор в сфере [Отрасль], работает на основе партнерских отношений
- **[Коннектор G]** - Коннектор [Отрасль] (представил [Контакт X]/[Компания F])

**Интеграция с управлением контактами**:

- Статус коннектора отслеживается в индивидуальных профилях контактов (`Тип контакта: Коннектор`)
- Детали сети интродукций поддерживаются в `Contacts/[Имя]/Intro_list.md`
- Запросы на интродукции управляются через `Contacts_Actual.md` [70KB]
- Регулярные обновления индивидуальных списков интродукций по мере развития сети

---

## Git операции

**Участникам следует избегать использования символов `<>` в именах файлов** для совместимости с Windows

- Агент будет обрабатывать любые файлы со специальными символами, которые проскользнут

---

## Справочник устаревших скриптов (Использовать с осторожностью)

⚠️ **ПРЕДУПРЕЖДЕНИЕ**: Эти скрипты могут быть устаревшими и, вероятно, будут обновлены в будущем. Используйте эти инструкции осторожно и убедитесь, что они все еще работают.

### Доступные скрипты в `MainCRM/Scripts/Old/`

- Если пользователь просит "сгенерировать исчерпывающие синонимы/вариации имен для людей для помощи в поиске email" или "создать регулярные выражения для поиска email по имени":
  - Используйте скрипт `MainCRM/Scripts/Old/generate_name_synonyms.py`
  - Запустите из `MainCRM/Scripts/Old`:
    - `python generate_name_synonyms.py`
  - Входы: `people.txt` в той же папке
  - Выходы: `name_synonyms.md`, `email_search_patterns.md`

- Если пользователь просит "создать people_with_synonyms.txt, используя только фиксированные уменьшительные (без нечеткого поиска)" или "развернуть уменьшительные имена в альтернативные строки":
  - Используйте скрипт `MainCRM/Scripts/Old/create_people_synonyms.py`
  - Запустите из `MainCRM/Scripts/Old`:
    - `python create_people_synonyms.py`
  - Входы: `people.txt`
  - Выходы: `people_with_synonyms.txt` (+ сводка в конце)

- Если пользователь просит "очистить дублирующиеся разделы email Gmail в файлах .md людей, оставить только последнюю загрузку" или "удалить повторяющиеся разделы 'Gmail Emails - Downloaded'":
  - Используйте скрипт `MainCRM/Scripts/Old/cleanup_duplicate_emails.py`
  - Запустите из `MainCRM/Scripts/Old`:
    - Сухой запуск: `python cleanup_duplicate_emails.py --dry-run --dir ../People_from_Notion`
    - Применить: `python cleanup_duplicate_emails.py --dir ../People_from_Notion`
  - Поведение: обнаруживает несколько разделов, начинающихся с `## Gmail Emails - Downloaded YYYY-MM-DD HH:MM:SS`, сохраняет последний, удаляет старые

- Если пользователь просит "проанализировать перекрестные ссылки/упоминания между файлами контактов .md" или "построить простую карту зависимостей markdown, упоминающих другие markdown":
  - Используйте скрипт `MainCRM/Scripts/Old/analyze_dependencies.py`
  - Запустите из `MainCRM/Scripts/Old`:
    - `python analyze_dependencies.py`
  - Выход: `dependencies_report.md`, перечисляющий `A.md -> B.md, C.md` на основе упоминаний

- Если пользователь просит "удалить повторяющееся содержимое внутри блоков кода полного содержимого email в файлах .md людей" или "удалить дублирующиеся абзацы/строки внутри огороженных блоков после **Полное содержимое email":
  - Используйте скрипт `MainCRM/Scripts/Old/dedupe_md_full_email_content.py`
  - Запустите из `MainCRM/Scripts/Old`:
    - `python dedupe_md_full_email_content.py`
  - Входы: `people_emails.json` для списка людей; работает с соответствующими файлами `Person Name.md` в той же директории
  - Поведение: внутри огороженных блоков, следующих за строками, начинающимися с `**Full email content`, сначала удаляет дублирующиеся абзацы, затем дублирующиеся строки; сохраняет форматирование, где это возможно
