# 🔧 Utility Scripts

Полезные скрипты для автоматизации проверок и вычислений.

## 📊 check_range.py

Проверяет, попадают ли числа в заданные диапазоны. Полезно для анализов здоровья, финансовых показателей, и других числовых данных.

### Использование:
```bash
python3 scripts/check_range.py [значение] [мин] [макс] [значение] [мин] [макс] ...
```

### Примеры:

**Анализы крови:**
```bash
# Холестерин (норма 120-200), Сахар (норма 70-100), Давление (норма 80-120)
python3 scripts/check_range.py 185 120 200 92 70 100 85 80 120
```

**Бюджетные категории:**
```bash
# Расходы на еду (бюджет 500-700), Развлечения (бюджет 200-300)
python3 scripts/check_range.py 650 500 700 250 200 300
```

**Результат:**
```
🔍 Проверка диапазонов:
==================================================
   185.0 | [ 120.0 -  200.0] | ✅ В диапазоне
    92.0 | [  70.0 -  100.0] | ✅ В диапазоне
    85.0 | [  80.0 -  120.0] | ✅ В диапазоне
==================================================
```

### Зачем это нужно?

LLM часто ошибаются при сравнении чисел. Этот скрипт даёт точный математический результат, который можно использовать в анализе.

## 📧 outlook_email.py

Работа с Office 365 почтой через Microsoft Graph API. Чтение и отправка писем прямо из командной строки.

**⚠️ Требует регистрации приложения в Azure Portal и административных разрешений.**

## 📧 outlook_exchange.py (РЕКОМЕНДУЕТСЯ для Exchange)

Работа с Office 365 почтой через Exchange Web Services (EWS). **Не требует Azure и административных разрешений!**

### Преимущества:
- ✅ Не требует Azure Portal
- ✅ Не требует административных разрешений
- ✅ Использует нативный протокол Exchange
- ✅ Работает с корпоративными аккаунтами
- ✅ Автоматическое определение сервера (autodiscover)

### Настройка:

1. Установите библиотеку:
   ```bash
   pip3 install exchangelib
   ```

2. Создайте пароль приложения (если включена 2FA):
   - [account.microsoft.com/security](https://account.microsoft.com/security)
   - Безопасность → Пароли приложений

3. Запустите скрипт для создания конфигурации:
   ```bash
   python3 scripts/outlook_exchange.py list
   ```

4. Заполните `scripts/outlook_exchange_config.json` с вашими данными

**Подробная инструкция:** `scripts/outlook_exchange_setup.md`

### Использование:

```bash
# Просмотр писем
python3 scripts/outlook_exchange.py list --limit 10

# Чтение письма
python3 scripts/outlook_exchange.py read --index 0

# Отправка письма
python3 scripts/outlook_exchange.py send \
  --to "recipient@example.com" \
  --subject "Тема" \
  --body "Текст письма" \
  --attach "/path/to/file.pdf"
```

## 📧 outlook_imap.py

Работа с Office 365 почтой через IMAP/SMTP. **Не требует Azure и административных разрешений!**

### Преимущества:
- ✅ Не требует Azure Portal
- ✅ Не требует административных разрешений
- ✅ Работает с личными и корпоративными аккаунтами
- ✅ Простая настройка

### Настройка:

1. Включите IMAP в настройках Outlook (если не включен)
2. Создайте пароль приложения (если включена 2FA):
   - [account.microsoft.com/security](https://account.microsoft.com/security)
   - Безопасность → Пароли приложений
3. Запустите скрипт для создания конфигурации:
   ```bash
   python3 scripts/outlook_imap.py list
   ```
4. Заполните `scripts/outlook_imap_config.json` с вашими данными

**Подробная инструкция:** `scripts/outlook_imap_setup.md`

### Использование:

```bash
# Просмотр писем
python3 scripts/outlook_imap.py list --limit 10

# Чтение письма
python3 scripts/outlook_imap.py read --index 0

# Отправка письма
python3 scripts/outlook_imap.py send \
  --to "recipient@example.com" \
  --subject "Тема" \
  --body "Текст письма" \
  --attach "/path/to/file.pdf"
```

### Настройка:

1. Зарегистрируйте приложение в Azure Portal
2. Получите Client ID и Client Secret
3. Настройте разрешения (Mail.Read, Mail.Send)
4. Заполните `scripts/outlook_config.json`

**Подробная инструкция:** См. `scripts/outlook_setup.md` или `scripts/QUICK_START_OUTLOOK.md`

### Использование:

```bash
# Просмотр списка писем
python3 scripts/outlook_email.py list --limit 10

# Чтение письма
python3 scripts/outlook_email.py read --index 0

# Отправка письма
python3 scripts/outlook_email.py send \
  --to "recipient@example.com" \
  --subject "Тема" \
  --body "Текст письма" \
  --attach "/path/to/file.pdf"

# Поиск писем
python3 scripts/outlook_email.py search --query "важно"
```

### Безопасность:

- Файл `outlook_config.json` автоматически добавлен в `.gitignore`
- Client Secret не будет закоммичен в Git
- Не делитесь Client Secret с другими

## Идеи для других скриптов:

- `calculate_bmi.py` - расчёт индекса массы тела
- `currency_convert.py` - конвертация валют
- `compound_interest.py` - расчёт сложных процентов
- `health_metrics.py` - расчёт здоровых диапазонов по возрасту 