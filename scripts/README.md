# 🔧 Utility Scripts

Полезные скрипты для автоматизации проверок и вычислений.

## 📊 check_range.py

Проверяет, попадают ли числа в заданные диапазоны. Полезно для анализов здоровья, финансовых показателей, и других числовых данных.

### Использование:
```bash
python3 scripts/check_range.py [значение] [мин] [макс] [значение] [мин] [макс] ...
```

### Примеры:

**Анализы крови:**
```bash
# Холестерин (норма 120-200), Сахар (норма 70-100), Давление (норма 80-120)
python3 scripts/check_range.py 185 120 200 92 70 100 85 80 120
```

**Бюджетные категории:**
```bash
# Расходы на еду (бюджет 500-700), Развлечения (бюджет 200-300)
python3 scripts/check_range.py 650 500 700 250 200 300
```

**Результат:**
```
🔍 Проверка диапазонов:
==================================================
   185.0 | [ 120.0 -  200.0] | ✅ В диапазоне
    92.0 | [  70.0 -  100.0] | ✅ В диапазоне
    85.0 | [  80.0 -  120.0] | ✅ В диапазоне
==================================================
```

### Зачем это нужно?

LLM часто ошибаются при сравнении чисел. Этот скрипт даёт точный математический результат, который можно использовать в анализе.

## 📧 outlook_exchange.py

Работа с Office 365 почтой через Exchange Web Services (EWS). **Единственный поддерживаемый метод для работы с почтой.**

### Преимущества:
- ✅ Не требует Azure Portal
- ✅ Не требует административных разрешений
- ✅ Использует нативный протокол Exchange
- ✅ Работает с корпоративными аккаунтами
- ✅ Автоматическое определение сервера (autodiscover)

### Настройка:

1. Установите библиотеку:
   ```bash
   pip3 install exchangelib
   ```

2. Создайте пароль приложения (если включена 2FA):
   - [account.microsoft.com/security](https://account.microsoft.com/security)
   - Безопасность → Пароли приложений

3. Запустите скрипт для создания конфигурации:
   ```bash
   python3 scripts/outlook_exchange.py list
   ```

4. Заполните `scripts/outlook_exchange_config.json` с вашими данными

**Подробная инструкция:** `scripts/outlook_exchange_setup.md`

### Использование:

```bash
# Просмотр писем
python3 scripts/outlook_exchange.py list --limit 10

# Чтение письма
python3 scripts/outlook_exchange.py read --index 0

# Чтение из папки "Отправленные"
python3 scripts/outlook_exchange.py read --index 0 --folder sent

# Отправка письма
python3 scripts/outlook_exchange.py send \
  --to "recipient@example.com" \
  --subject "Тема" \
  --body "Текст письма" \
  --attach "/path/to/file.pdf"

# Поиск писем
python3 scripts/outlook_exchange.py search --query "profitbase" --limit 20
```

### Безопасность:

- Файл `outlook_exchange_config.json` автоматически добавлен в `.gitignore`
- Пароль не будет закоммитен в Git
- Не делитесь паролем с другими

## 📅 Работа с календарем Exchange

### Просмотр событий календаря:

```bash
# Просмотр ближайших событий (по умолчанию - следующие 30 дней)
python3 scripts/outlook_exchange.py calendar

# Просмотр с ограничением количества
python3 scripts/outlook_exchange.py calendar --limit 20

# Просмотр событий за конкретный период
python3 scripts/outlook_exchange.py calendar --start "2025-12-01" --end "2025-12-31"
```

### Создание встречи:

```bash
# Создание встречи
python3 scripts/outlook_exchange.py calendar-create \
  --subject "Встреча с Profitbase" \
  --start "2025-12-24 09:00" \
  --end "2025-12-24 10:00" \
  --attendees "nshirobokova@profitbase.ru" \
  --body "Обсуждение интеграции API" \
  --location "Онлайн"

# Создание встречи с несколькими участниками
python3 scripts/outlook_exchange.py calendar-create \
  --subject "Командная встреча" \
  --start "2025-12-25 14:00" \
  --end "2025-12-25 15:00" \
  --attendees "user1@example.com,user2@example.com"
```

### Поиск событий:

```bash
# Поиск событий по ключевому слову
python3 scripts/outlook_exchange.py calendar-search --query "Profitbase"

# Поиск с ограничением периода
python3 scripts/outlook_exchange.py calendar-search \
  --query "встреча" \
  --start "2025-12-01" \
  --end "2025-12-31" \
  --limit 10
```

### Форматы дат:

- `YYYY-MM-DD HH:MM` - например: `2025-12-24 09:00`
- `YYYY-MM-DD` - дата без времени (время будет 00:00)
- `YYYY-MM-DDTHH:MM:SS` - ISO формат

## Идеи для других скриптов:

- `calculate_bmi.py` - расчёт индекса массы тела
- `currency_convert.py` - конвертация валют
- `compound_interest.py` - расчёт сложных процентов
- `health_metrics.py` - расчёт здоровых диапазонов по возрасту 